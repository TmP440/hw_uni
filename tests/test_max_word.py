from tasks.i12_max_word.max_word import *


def test_max_word():
    # Вставьте нужные файлы
    files = [..., ...]
    for filename in files:
        print(read_file_and_find_max_length_word(filename))
